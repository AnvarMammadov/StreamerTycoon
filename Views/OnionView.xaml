<UserControl x:Class="StreamerTycoon.Views.OnionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#0A0A0A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="TOR CIRCUIT: ESTABLISHED" Foreground="#00FF00" FontFamily="Consolas" Margin="15,10" FontSize="14"/>
        <ScrollViewer Grid.Row="1" Margin="15">
            <ItemsControl ItemsSource="{Binding HackingTools}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#333" BorderThickness="1" Background="#111" Margin="0,5" Padding="10" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding}" Foreground="#AB47BC" FontFamily="Consolas" FontSize="14" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="EXECUTE" Background="Transparent" Foreground="#00FF00" BorderBrush="#00FF00" BorderThickness="1" 
                                        FontFamily="Consolas" Padding="10,2" Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>