<UserControl x:Class="StreamerTycoon.Views.OnionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#0A0A0A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1">
            <StackPanel Margin="15,10">
                <TextBlock Text="TOR CIRCUIT: ESTABLISHED" Foreground="#00FF00" FontFamily="Consolas" FontSize="14"/>
                <TextBlock Text="DARK WEB MARKETPLACE" Foreground="#AB47BC" FontFamily="Consolas" FontSize="10" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="15,10">
            <ItemsControl ItemsSource="{Binding HackingTools}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#333" BorderThickness="1" Background="#111" Margin="0,5" Padding="10" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Foreground="#AB47BC" FontFamily="Consolas" FontSize="14" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Description}" Foreground="#666" FontSize="10" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Price, StringFormat='${0:N0}'}" Foreground="#FF453A" FontFamily="Consolas" FontSize="14" Margin="0,0,15,0" VerticalAlignment="Center"/>

                                    <Button Content="BUY" Background="Transparent" Foreground="#00FF00" BorderBrush="#00FF00" BorderThickness="1" 
                                            Command="{Binding DataContext.BuyToolCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            FontFamily="Consolas" Padding="10,2" Cursor="Hand"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#000" BorderBrush="#333" BorderThickness="0,1,0,0" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ConsoleLog}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="#00FF00" FontFamily="Consolas" FontSize="10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>