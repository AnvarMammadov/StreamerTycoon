<UserControl x:Class="StreamerTycoon.Views.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">

    <Grid Background="#111827">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#1F2937" BorderBrush="#374151" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="G2" Foreground="#EF4444" FontWeight="Bold" FontSize="20" FontStyle="Italic"/>
                    <TextBlock Text="MARKET" Foreground="White" FontWeight="Bold" FontSize="20"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#111827" CornerRadius="18" Height="36" Margin="20,0" BorderBrush="#374151" BorderThickness="1">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                        <TextBlock Text="🔒" Foreground="#10B981" Margin="0,0,8,0" FontSize="10"/>
                        <TextBlock Text="https://g2g.market/accounts/valorant" Foreground="#9CA3AF" FontSize="12"/>
                    </StackPanel>
                </Border>

                <Ellipse Grid.Column="2" Width="32" Height="32" Fill="#374151" Margin="0,0,20,0"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="20">
            <ItemsControl ItemsSource="{Binding MarketItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="220" Height="280" Margin="10" Background="#1F2937" CornerRadius="8" Cursor="Hand">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.3" ShadowDepth="2"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="140"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="50"/>
                                </Grid.RowDefinitions>

                                <Border Background="#374151" CornerRadius="8,8,0,0" ClipToBounds="True">
                                    <Grid>
                                        <Path Data="{StaticResource Icon_User}" Fill="#4B5563" Stretch="Uniform" Width="60" Opacity="0.5"/>

                                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" CornerRadius="4" Padding="8,4">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#1F2937"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RiskLevel}" Value="High">
                                                            <Setter Property="Background" Value="#DC2626"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding RiskLevel}" Value="Medium">
                                                            <Setter Property="Background" Value="#F59E0B"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding RiskLevel}" Value="Low">
                                                            <Setter Property="Background" Value="#10B981"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding RiskLevel}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>

                                <StackPanel Grid.Row="1" Margin="12,10">
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Type}" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Ellipse Width="16" Height="16" Fill="#4B5563"/>
                                        <TextBlock Text="{Binding SellerName}" Foreground="#60A5FA" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Row="2" BorderBrush="#374151" BorderThickness="0,1,0,0" Padding="12,0">
                                    <Grid>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Price" Foreground="#9CA3AF" FontSize="10"/>
                                            <TextBlock Text="{Binding Price, StringFormat='${0}'}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                        </StackPanel>

                                        <Button Grid.Row="2" 
        Command="{Binding DataContext.BuyItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
        CommandParameter="{Binding}"
        Content="{Binding Price, StringFormat='BUY - ${0}'}" 
        Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#2563EB"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>