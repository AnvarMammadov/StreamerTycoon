<UserControl x:Class="StreamerTycoon.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="850">

    <Grid Background="#191919">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#202020" BorderBrush="#333" BorderThickness="0,0,0,1" Padding="15,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="←" Foreground="#AAA" FontSize="18" Margin="0,0,15,0" Cursor="Hand"/>
                    <TextBlock Text="→" Foreground="#555" FontSize="18" Margin="0,0,15,0"/>
                    <TextBlock Text="↑" Foreground="#AAA" FontSize="18" Cursor="Hand"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#2C2C2C" CornerRadius="4" Height="32" VerticalAlignment="Center" Margin="0,0,15,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource Icon_Folder}" Fill="#AAA" Width="14" Height="14" Stretch="Uniform" Margin="8"/>
                        <TextBlock Grid.Column="1" Text="{Binding CurrentPath}" Foreground="White" VerticalAlignment="Center" FontSize="12"/>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Background="#2C2C2C" CornerRadius="4" Height="32" VerticalAlignment="Center">
                    <Grid>
                        <TextBlock Text="Search" Foreground="#777" VerticalAlignment="Center" Margin="10,0"/>
                        <TextBlock Text="🔍" Foreground="#AAA" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#191919" BorderBrush="#333" BorderThickness="0,0,1,0" Padding="10">
                <StackPanel>
                    <TextBlock Text="📌 Quick Access" Foreground="#AAA" FontSize="11" Margin="5,10,0,5" FontWeight="Bold"/>

                    <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand">
                        <Path Data="{StaticResource Icon_Folder}" Fill="#4CC2FF" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="Desktop" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand">
                        <Path Data="{StaticResource Icon_Folder}" Fill="#FFD54F" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="Downloads" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand">
                        <Path Data="{StaticResource Icon_Folder}" Fill="#FFD54F" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="Documents" Foreground="White"/>
                    </StackPanel>

                    <TextBlock Text="💻 This PC" Foreground="#AAA" FontSize="11" Margin="5,20,0,5" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand">
                        <Path Data="{StaticResource Icon_MyPC}" Fill="#AAA" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="Local Disk (C:)" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand">
                        <Path Data="{StaticResource Icon_Trash}" Fill="#AAA" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="Recycle Bin" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Column="1" Padding="10">
                <ItemsControl ItemsSource="{Binding Files}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="100" Height="110" Margin="5" Background="Transparent" CornerRadius="4" Cursor="Hand">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2C2C2C"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="40"/>
                                    </Grid.RowDefinitions>

                                    <Path Data="{StaticResource Icon_Folder}" Stretch="Uniform" Width="48" Height="48" VerticalAlignment="Bottom" Margin="0,0,0,5">
                                        <Path.Style>
                                            <Style TargetType="Path">
                                                <Setter Property="Fill" Value="#FFD54F"/>
                                                <Setter Property="Data" Value="{StaticResource Icon_Folder}"/>

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="Text">
                                                        <Setter Property="Fill" Value="#AAA"/>
                                                        <Setter Property="Data" Value="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Application">
                                                        <Setter Property="Fill" Value="#007ACC"/>
                                                        <Setter Property="Data" Value="{StaticResource Icon_Start}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Image">
                                                        <Setter Property="Fill" Value="#BF5AF2"/>
                                                        <Setter Property="Data" Value="M8.5,13.5L11,16.5L14.5,12L19,18H5M21,19V5C21,3.89 20.1,3 19,3H5A2,2 0 0,0 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19Z"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Path.Style>
                                    </Path>

                                    <TextBlock Grid.Row="1" Text="{Binding Name}" Foreground="#EEE" 
                                               TextAlignment="Center" TextWrapping="Wrap" FontSize="11" 
                                               Margin="5,0" VerticalAlignment="Top"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="2" Background="#202020" BorderBrush="#333" BorderThickness="0,1,0,0" Padding="10,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding Files.Count, StringFormat='{}{0} items'}" Foreground="#AAA" FontSize="11"/>
                <Border Width="1" Height="12" Background="#444" Margin="10,0"/>
                <TextBlock Text="1 item selected" Foreground="#AAA" FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>