<UserControl x:Class="StreamerTycoon.Views.StreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="950">

    <Grid Background="#2B2E38">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F2128" BorderBrush="#111" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                <TextBlock Text="File" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Edit" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="View" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Docks" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Profile" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Scene Collection" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Tools" Foreground="#DDD" Margin="5,0" FontSize="11"/>
                <TextBlock Text="Help" Foreground="#DDD" Margin="5,0" FontSize="11"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#111" Margin="10">
                <Grid>
                    <Grid Opacity="0.5">
                        <Path Data="{StaticResource Icon_MyPC}" Fill="#333" Stretch="Uniform" Margin="100"/>
                        <TextBlock Text="NO SIGNAL" Foreground="#444" FontSize="30" FontWeight="Bold" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0"/>
                    </Grid>

                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLive}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Rectangle Fill="{StaticResource GeometricBackground}" Opacity="0.6"/>
                        <TextBlock Text="🔴 LIVE CAPTURE" Foreground="#FF453A" Margin="15" FontSize="14" FontWeight="Bold" FontFamily="Consolas"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#1F2128" Margin="0,10,10,10" BorderBrush="#111" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Background="#2B2E38" BorderBrush="#111" BorderThickness="0,0,0,1">
                        <TextBlock Text="STREAM CHAT" Foreground="#AAA" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10" FontWeight="Bold"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="5">
                        <ItemsControl ItemsSource="{Binding ChatMessages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock TextWrapping="Wrap" Margin="0,2" FontSize="11" FontFamily="Segoe UI">
                                        <Run Text="{Binding Username}" Foreground="{Binding ColorHex}" FontWeight="Bold"/>
                                        <Run Text=":" Foreground="#888"/>
                                        <Run Text="{Binding Message}" Foreground="#DDD"/>
                                    </TextBlock>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="2" Background="#181A1F" Height="100" BorderBrush="#333" BorderThickness="0,1,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="ACTIVITY FEED" Foreground="#666" FontSize="9" Margin="5" FontWeight="Bold"/>
                            <ScrollViewer Grid.Row="1">
                                <ItemsControl ItemsSource="{Binding ActivityFeed}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="5,2">
                                                <Ellipse Width="6" Height="6" Fill="{Binding Color}" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Message}" Foreground="White" FontSize="10"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Background="#2B2E38" Margin="10,0,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1F2128" Margin="0,0,5,0" BorderBrush="#111" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Scenes" Foreground="#DDD" VerticalAlignment="Center" Margin="5,0" FontSize="11" FontWeight="Bold"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Scenes}" Background="Transparent" BorderThickness="0" Foreground="White" SelectedIndex="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Name="Border" Background="Transparent" SnapsToDevicePixels="true">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="true">
                                                    <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Background="#2B2E38">
                        <TextBlock Text="+" Foreground="#AAA" Margin="10,0" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="-" Foreground="#AAA" Margin="5,0" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#1F2128" Margin="0,0,5,0" BorderBrush="#111" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="24"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Sources" Foreground="#DDD" VerticalAlignment="Center" Margin="5,0" FontSize="11" FontWeight="Bold"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Sources}" Background="Transparent" BorderThickness="0" Foreground="White" SelectedIndex="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Name="Border" Background="Transparent" SnapsToDevicePixels="true">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="true">
                                                    <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Background="#2B2E38">
                        <TextBlock Text="+" Foreground="#AAA" Margin="10,0" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="-" Foreground="#AAA" Margin="5,0" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="#1F2128" Margin="0,0,5,0" BorderBrush="#111" BorderThickness="1">
                <Grid>
                    <TextBlock Text="Audio Mixer" Foreground="#DDD" VerticalAlignment="Top" Margin="5" FontSize="11" FontWeight="Bold"/>
                    <StackPanel VerticalAlignment="Center" Margin="10">
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Desktop" Foreground="#AAA" FontSize="10" VerticalAlignment="Center" Width="45"/>
                            <ProgressBar Grid.Column="1" Height="8" Value="75" Background="#111" BorderThickness="0" Foreground="#00E676"/>
                            <TextBlock Grid.Column="2" Text="-5dB" Foreground="White" FontSize="9" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Mic/Aux" Foreground="#AAA" FontSize="10" VerticalAlignment="Center" Width="45"/>
                            <ProgressBar Grid.Column="1" Height="8" Value="40" Background="#111" BorderThickness="0" Foreground="#00E676"/>
                            <TextBlock Grid.Column="2" Text="-12dB" Foreground="White" FontSize="9" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="3" Background="#1F2128" BorderBrush="#111" BorderThickness="1" Padding="10">
                <StackPanel VerticalAlignment="Center">
                    <Button Content="Start Streaming" Command="{Binding ToggleStreamCommand}" Height="35" Margin="0,2" Background="#111" Foreground="#DDD" BorderThickness="0" FontSize="12">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Name="bdr" Background="#333" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsLive}" Value="True">
                                                    <Setter Property="Content" Value="Stop Streaming"/>
                                                    <Setter TargetName="bdr" Property="Background" Value="#D32F2F"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bdr" Property="Background" Value="#444"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Start Recording" Height="30" Margin="0,2" Background="Transparent" Foreground="#AAA" BorderThickness="0"/>
                    <Button Content="Studio Mode" Height="30" Margin="0,2" Background="Transparent" Foreground="#AAA" BorderThickness="0"/>
                    <Button Content="Settings" Height="30" Margin="0,2" Background="Transparent" Foreground="#AAA" BorderThickness="0"/>
                    <Button Content="Exit" Height="30" Margin="0,2" Background="Transparent" Foreground="#AAA" BorderThickness="0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="#1F2128" BorderBrush="#111" BorderThickness="0,1,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="LIVE: " Foreground="#888" FontSize="11"/>
                    <TextBlock Text="{Binding StreamDuration}" Foreground="White" Margin="0,0,15,0" FontSize="11"/>

                    <TextBlock Text="REC: 00:00:00" Foreground="#888" Margin="0,0,15,0" FontSize="11"/>
                    <TextBlock Text="{Binding CpuUsage, StringFormat='CPU: {0}'}" Foreground="#888" Margin="0,0,15,0" FontSize="11"/>
                    <TextBlock Text="{Binding Fps, StringFormat='FPS: {0}'}" Foreground="#888" Margin="0,0,15,0" FontSize="11"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0">
                    <Rectangle Width="10" Height="10" Fill="#00E676" Margin="0,0,5,0" RadiusX="1" RadiusY="1">
                        <Rectangle.Style>
                            <Style TargetType="Rectangle">
                                <Setter Property="Fill" Value="#555"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLive}" Value="True">
                                        <Setter Property="Fill" Value="#00E676"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Rectangle.Style>
                    </Rectangle>
                    <TextBlock Text="{Binding Bitrate}" Foreground="#DDD" FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>