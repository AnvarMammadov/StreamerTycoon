<UserControl x:Class="StreamerTycoon.Views.StreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="850">
    
    <Grid Background="#0B0E11">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Border Background="#1A1D21" CornerRadius="4" Padding="10,5" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#555"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLive}" Value="True">
                                            <Setter Property="Fill" Value="#FF453A"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding IsLive, StringFormat='STATUS: {0}'}" Foreground="#CCC" FontWeight="Bold" FontSize="11"/>
                    </StackPanel>
                </Border>
                <Border Background="#1A1D21" CornerRadius="4" Padding="10,5" Margin="0,0,10,0">
                    <TextBlock Text="{Binding StreamDuration, StringFormat='TIME: {0}'}" Foreground="#00E676" FontFamily="Consolas" FontWeight="Bold"/>
                </Border>
                <Border Background="#1A1D21" CornerRadius="4" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👥" Margin="0,0,5,0" Foreground="#AAA"/>
                        <TextBlock Text="{Binding ViewerCount}" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Row="1" Background="Black" BorderBrush="#333" BorderThickness="2" CornerRadius="4" Margin="0,10">
                <Grid>
                    <Grid Opacity="0.3">
                        <Path Data="{StaticResource Icon_MyPC}" Fill="#333" Stretch="Uniform" Margin="100"/>
                    </Grid>
                    
                    <TextBlock Text="OFFLINE" Foreground="#333" FontSize="40" FontWeight="Bold" 
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLive}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLive}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style> <Rectangle Fill="{StaticResource GeometricBackground}" Opacity="0.5"/>
                        <TextBlock Text="🎥 GAME CAPTURE: [Desktop]" Foreground="#00E676" Margin="10" VerticalAlignment="Top" FontFamily="Consolas"/>
                        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="20" Background="#80000000" Padding="10" CornerRadius="4">
                            <TextBlock Text="Latest Event: New Follower!" Foreground="White" FontSize="12"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                <Button Command="{Binding ToggleStreamCommand}" Width="150" Height="40" Margin="5" Cursor="Hand" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Name="bdr" CornerRadius="4" Background="#1A1D21">
                                            <TextBlock Name="txt" Text="START STREAM" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLive}" Value="True">
                                                <Setter TargetName="bdr" Property="Background" Value="#FF453A"/>
                                                <Setter TargetName="txt" Property="Text" Value="STOP STREAM"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bdr" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#00E676" BlurRadius="10" Opacity="0.4"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="#14171A" BorderBrush="#222" BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="150"/>
                </Grid.RowDefinitions>
                <Border BorderBrush="#222" BorderThickness="0,0,0,1">
                    <TextBlock Text="STREAM CHAT" Foreground="#AAA" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                </Border>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                    <ItemsControl ItemsSource="{Binding ChatMessages}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel VerticalAlignment="Bottom"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock TextWrapping="Wrap" Margin="10,2" FontSize="12">
                                    <Run Text="{Binding Username}" Foreground="{Binding ColorHex}" FontWeight="Bold"/>
                                    <Run Text=": " Foreground="#555"/>
                                    <Run Text="{Binding Message}" Foreground="#DDD"/>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <Border Grid.Row="2" Background="#0F1114" BorderBrush="#222" BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="ACTIVITY FEED" Foreground="#666" FontSize="10" Margin="10,8"/>
                        <ScrollViewer Grid.Row="1">
                            <ItemsControl ItemsSource="{Binding ActivityFeed}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="5,2" Background="#1A1D21" CornerRadius="4" Padding="5">
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="6" Height="6" Fill="{Binding Color}" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Message}" Foreground="White" FontSize="11" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>