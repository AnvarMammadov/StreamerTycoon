<UserControl x:Class="StreamerTycoon.Views.DesktopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1280">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0F2027" Offset="0.0"/>
                    <GradientStop Color="#203A43" Offset="0.5"/>
                    <GradientStop Color="#2C5364" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Path Data="M0,0 L1280,800 M0,800 L1280,0" Stroke="#33FFFFFF" StrokeThickness="1" IsHitTestVisible="False"/>
            <Ellipse Width="400" Height="400" Fill="#10FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-100"/>

            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20">

                <Button Style="{StaticResource TaskbarBtn}" Width="80" Height="90" Margin="0,0,0,10">
                    <StackPanel>
                        <Viewbox Width="40" Height="40" Margin="0,0,0,5">
                            <Path Data="{StaticResource Icon_Browser}" Fill="#4285F4"/>
                        </Viewbox>
                        <TextBlock Text="G2 Browser" Foreground="White" FontSize="11" TextAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource TaskbarBtn}" Width="80" Height="90" Margin="0,0,0,10">
                    <StackPanel>
                        <Viewbox Width="40" Height="40" Margin="0,0,0,5">
                            <Path Data="{StaticResource Icon_Onion}" Fill="#8e44ad"/>
                        </Viewbox>
                        <TextBlock Text="Thor Browser" Foreground="White" FontSize="11" TextAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource TaskbarBtn}" Width="80" Height="90">
                    <StackPanel>
                        <Viewbox Width="40" Height="40" Margin="0,0,0,5">
                            <Path Data="{StaticResource Icon_Discord}" Fill="#5865F2"/>
                        </Viewbox>
                        <TextBlock Text="Discurse" Foreground="White" FontSize="11" TextAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="1"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Button>

            </StackPanel>

            <Grid x:Name="ActiveAppContainer" Margin="50"/>
        </Grid>

        <Border Grid.Row="1" Background="#CC101010" BorderBrush="#333" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Width="50" Height="50" Style="{StaticResource TaskbarBtn}">
                    <Viewbox Width="20" Height="20">
                        <Path Data="{StaticResource Icon_Windows}" Fill="#00ADEF"/>
                    </Viewbox>
                </Button>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0">
                    <Button Style="{StaticResource TaskbarBtn}" Margin="2">
                        <Viewbox Width="20" Height="20">
                            <Path Data="{StaticResource Icon_Browser}" Fill="#BBB"/>
                        </Viewbox>
                    </Button>
                    <Button Style="{StaticResource TaskbarBtn}" Margin="2">
                        <Viewbox Width="20" Height="20">
                            <Path Data="{StaticResource Icon_Onion}" Fill="#BBB"/>
                        </Viewbox>
                    </Button>
                    <Button Style="{StaticResource TaskbarBtn}" Margin="2">
                        <Viewbox Width="20" Height="20">
                            <Path Data="{StaticResource Icon_Discord}" Fill="#BBB"/>
                        </Viewbox>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,20,0">
                    <TextBlock Text="{Binding CurrentTime}" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding CurrentDate}" Foreground="#BBB" FontSize="10" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>