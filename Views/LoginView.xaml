<UserControl x:Class="StreamerTycoon.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1280">

    <Grid>
        <Grid.Background>
            <RadialGradientBrush GradientOrigin="0.5,0.8" Center="0.5,0.5" RadiusX="0.8" RadiusY="0.8">
                <GradientStop Color="#0078D4" Offset="0"/>
                <GradientStop Color="#502D7F" Offset="0.6"/>
                <GradientStop Color="#111111" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>

        <Rectangle Fill="Black" Opacity="0.2"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <Border Width="120" Height="120" CornerRadius="60" Background="#20FFFFFF" Margin="0,0,0,20" BorderBrush="#50FFFFFF" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.4" ShadowDepth="5"/>
                </Border.Effect>
                <Viewbox Margin="30">
                    <Path Data="{StaticResource Icon_User}" Fill="White"/>
                </Viewbox>
            </Border>

            <TextBlock Text="{Binding UserName}" Foreground="White" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource ModernTextBox}"
                     Margin="0,0,0,10"/>

            <TextBlock Text="{Binding LoginError}" Foreground="#FF8888" HorizontalAlignment="Center" FontSize="12" Margin="0,5,0,0"/>

            <Button Command="{Binding LoginCommand}" Content="Sign In" 
                    Background="#007ACC" Foreground="White" 
                    Margin="0,20,0,0" Padding="10" BorderThickness="0" FontWeight="Bold" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="60,0,0,60">
            <TextBlock Text="{Binding CurrentTime}" Foreground="White" FontSize="72" FontWeight="Thin">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.3"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="{Binding CurrentDate}" Foreground="#DDDDDD" FontSize="24" Margin="5,0,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5" Opacity="0.3"/>
                </TextBlock.Effect>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>