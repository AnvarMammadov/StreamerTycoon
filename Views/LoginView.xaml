<UserControl x:Class="StreamerTycoon.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1280">

    <Grid Background="{StaticResource GeometricBackground}">
        <Rectangle Fill="#BB000000"/>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <Grid Width="120" Height="120" Margin="0,0,0,20">
                <Ellipse Fill="#20FFFFFF" Stroke="#30FFFFFF" StrokeThickness="1"/>
                <Path Data="{StaticResource Icon_User}" Fill="White" Width="50" Height="50" Stretch="Uniform" Opacity="0.8"/>
            </Grid>

            <TextBlock Text="{Binding UserName}" Foreground="White" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,30" FontWeight="Light"/>

            <Border Background="#30000000" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="4" Width="250" Padding="5">
                <Grid>
                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                             Background="Transparent" BorderThickness="0" Foreground="White" 
                             HorizontalContentAlignment="Center" FontSize="16" Padding="5" CaretBrush="White"/>

                    <TextBlock Text="Password" IsHitTestVisible="False" Foreground="#60FFFFFF" 
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Password.Length, FallbackValue=0}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Password.Length, FallbackValue=1}" Value="1">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>

            <Button Command="{Binding LoginCommand}" Content="->" Width="50" Height="40" Margin="0,20,0,0" 
                    Background="#3A6B8E" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="18" FontWeight="Bold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <TextBlock Text="{Binding LoginError}" Foreground="#FF6B6B" HorizontalAlignment="Center" Margin="0,15,0,0"/>

        </StackPanel>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="30">
            <TextBlock Text="{Binding CurrentTime}" Foreground="White" FontSize="48" FontWeight="Thin" Opacity="0.8"/>
        </StackPanel>
    </Grid>
</UserControl>